{% extends '@OptimeEmail/layout.html.twig' %}

{# {% block page_header 'Email Templates Bundle' %} #}
{% block page_header_container '' %}

{% block body_content %}
    <div class="container-fluid mt-4 px-4">
        <div id="emails_config_root"
             data-locale="{{ app.locale }}"
             data-locales="{{ locales|json_encode }}"
             data-basename="{{ path('optime_emails_home') }}"
             data-api-url="{{ path('optime_emails_api_url') }}"
        ></div>
    </div>

    {{ include('@OptimeEmail/_import_codemirror_for_react.html.twig') }}
    <script defer src="{{ asset('bundles/optimeemail/build/app.js') }}"></script>
{% endblock %}